apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
    name: cloudflareddns
    namespace: cloudflareddns
spec:
    interval: 15m
    chart:
        spec:
            chart: cloudflareddns
            version: 14.9.1
            sourceRef:
                kind: HelmRepository
                name: truecharts
                namespace: flux-system
    values:
        # for now until image is fixed
        image:
            repository: ghcr.io/hotio/cloudflareddns
            pullPolicy: IfNotPresent
            tag: latest@sha256:61c87d2d714bd9c2b3ba8fc7a4c84cfe92256bc17b8bf74518067eaf71f2f63a
        securityContext:
            container:
                runAsNonRoot: false
                readOnlyRootFilesystem: false
                runAsUser: 0
                runAsGroup: 0
        TZ: Europe/Berlin
#        cloudflareddns:
#            api_token: ${CF_DDNS_API_TOKEN}
#            host_zone_record:
#                - domain: ${CF_DDNS_DOMAIN}
#                  record: A
#                  zone: ${CF_DDNS_ZONE}
        workload:
            main:
                podSpec:
                    containers:
                        main:
                            env:
                                UPDATE_IPV6: false
                                CF_APITOKEN: ${CF_DDNS_API_TOKEN}
                                CF_HOSTS: "ddns.${DOMAIN_0}"

